kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: my-quarkus-app
  labels:
    app: my-quarkus-app
spec:
  to:
    kind: Service
    name: my-quarkus-app
    weight: 100
  port:
    targetPort: '8081'