kind: Service
apiVersion: v1
metadata:
  name: my-quarkus-app
  labels:
    app: my-quarkus-app
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  selector:
    app: my-quarkus-app