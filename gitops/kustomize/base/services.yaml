kind: Service
apiVersion: v1
metadata:
  name: my-quarkus-app
  labels:
    app: my-quarkus-app
spec:
  ports:
    - name: http-8081
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
  selector:
    app: my-quarkus-app