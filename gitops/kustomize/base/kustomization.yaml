apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: my-quarkus-app

namespace: demo-app

resources:
  - deployments.yaml
  - services.yaml
  - routes.yaml

commonLabels:
  app: my-quarkus-app
  managed-by: kustomize
